on:
  push:
    branches:
      - master
jobs:
   build:
     runs-on: ubuntu-latest

     steps: 
        - uses: actions/checkout@v3
        # - name: "ls"
        #   run: echo rahma
        # - name: "all contant"
        #   run: ls   

                  
        - name: "Login"
          run: docker login --username ${{ secrets.DOCKER_USERNAME }} --password ${{ secrets.DOC_tokk}}
           
        - name: "Buliding image"
          run: docker build -t ${{ secrets.DOCKER_USERNAME }}/myimage .

        - name: "Pushing to Docker hub"
          run: docker push ${{ secrets.DOCKER_USERNAME }}/myimage 


